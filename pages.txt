Use the findings from responsibility-audit-form-layout-scroll.txt.

Proceed with implementation.

Requirements:

1. Keep body scrolling.

Do NOT introduce:

* overflow-y-auto containers
* nested scrolling regions
* h-screen form wrappers
* background-attachment: fixed

2. Remove the gradient from:

* app/(coordinator)/layout.tsx
* app/(facilitator)/layout.tsx

Replace with a neutral page background.

3. Add a fixed-height gradient hero section directly inside the form pages.

Use the existing QRMS Microsoft-inspired gradients.

Recommended height:

Desktop:
260px–300px

Mobile:
180px–220px

4. The hero section should contain:

* Project Name
* Quarterly Reports
* Quarter / Year
* Greeting

5. Add a compact sticky header.

Show only:

* Back button
* Project Name

Keep it minimal.

Do not include greeting text or metadata.

6. Keep body scrolling.

The hero section should scroll away naturally.

The sticky header should remain visible.

7. Use negative margin overlap so the form content slightly overlaps the hero section.

Microsoft Forms style.

8. Do not modify:

* form logic
* uploads
* validation
* API calls
* RBAC
* submit flow

9. Implement on coordinator form first.

After completion provide:

* Files modified
* Layout hierarchy after implementation
* Sticky header behavior
* Mobile behavior
* Screenshots or description of visual changes